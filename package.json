{
  "name": "chat-room",
  "version": "1.0.0",
  "description": "",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "run:client": "npm run dev --workspace=client",
    "run:server": "npm run dev --workspace=server"
  },
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.1.0"
  }
}
